homeassistant:
  name: Dev HA
  unit_system: metric
  time_zone: Europe/Kyiv

solar_inverter:
  device: fake
  scan_interval: 5
  queries: [qpigs, qpiri, qpiws]

logger:
  default: info
  logs:
    custom_components.solar_inverter: debug